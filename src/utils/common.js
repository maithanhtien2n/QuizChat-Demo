// import CryptoJS from 'crypto-js';

export const COMMON = {
    onEncryptedData: (data) => {
        // const result = CryptoJS.AES.encrypt(JSON.stringify(data), import.meta.env.VITE_BASE_JWT_SECRET).toString();
        return 'result';
    }
};
